# STATE

Где мы сейчас. Обновляется каждую итерацию. Единственный источник правды.

Last updated: 2026-09-17

## Done

**Импорт и состав закупки**
- Синхронизация ГосПлан: список закупок, детали, обогащение позициями,
  документами, условиями, заказчиком (gosplan_positions.py, 4 варианта упаковки)
- Восстановление ранее упавших тендеров (_ensure_tender_enriched)

**Обработка и отбор**
- Скоринг: реальная маржа вместо заглушки (КП, затем категория, затем настройки)
- Статусная машина замкнута: READY_FOR_DECISION достижим (tender_status_service.py)
- Защита ручных решений: APPROVED и REJECTED не переобрабатываются

**Поставщики и коммуникации**
- Фильтрация поставщиков: письма только с домена сайта, отсев маркетплейсов,
  сервисных доменов и непрофильных подразделений
- Напоминания не ответившим (send_reminders)
- Торг за цену (request_discounts)

**Автономность**
- Celery beat: 8 задач вместо 1
- Восстановление зависших задач, автообработка новых тендеров,
  автозапуск конвейера для лучших по скору
- worker-beat починен (пересобран, не было миграции 010)

**Инфраструктура**
- Ollama вынесена в отдельный сервис (tp-20260831-tender-ollama),
  проект подключается к http://199.189.253.227:11434
- Устранён дефект деплоя: правки через docker compose cp терялись
  при пересоздании контейнеров, теперь образы пересобираются

**Деплой**
- `scripts/deploy.sh` — идемпотентный деплой: проверки окружения, генерация
  секретов, сборка образов, запуск, ожидание готовности, создание первого токена
- `docs/DEPLOY.md` — подробный справочник (быстрый и ручной путь, обновление,
  бэкап БД, типовые проблемы, диагностика)
- `.env.example` в корне со всеми переменными и пояснениями

**Интерфейс**
- Виртуализация списка позиций: карточка тендера открывается даже при 11 014
  позициях (порции по 200 плюс кнопка «Показать ещё»); CSV отдаёт все позиции
- E2E-проверка в реальном браузере: frontend/e2e/live-card-check.cjs
  (запускается с хоста, не из контейнера)

**Память проекта**
- Структура docs/memory (PROJECT, STATE, DECISIONS, CONVENTIONS,
  GLOSSARY, BACKLOG) плюс AGENTS.md в корне
- RESEARCH.md и SESSION.md трансформированы в эту структуру

## In progress

Ничего не начато и не брошено. Предыдущая итерация завершена.

## Next

1. Автоотправка черновиков писем. В очереди около 240 готовых черновиков,
   отправка сейчас ручная через UI. Это главный затор воронки.
2. Разгрести тендеры в ERROR (около 1000). Восстановление идёт порциями по 20
   раз в 30 минут, нужен массовый прогон.
3. Реализовать 6 эндпоинтов, отдающих 404: таймлайн тендера, вебхуки,
   эмбеддинги, файлы, отмена задачи. Страницы фронта нерабочие.
4. Заменить заглушку S3 на реальный MinIO.
5. Учёт себестоимости: сейчас маржа считается от НМЦК, закупочная цена не вводится.

## Blockers

- Для полного цикла до READY_FOR_DECISION на реальных данных нужны ответы поставщиков.
  Сейчас КП всего 3, поэтому маржа сравнивается на малой выборке.
- Развёртывание repo сервиса Ollama на отдельном сервере ждёт адреса сервера.
  После этого меняется одна строка EMBEDDING_SERVICE_URL.

## Текущие цифры (2026-09-17)

- Тендеров: около 3600, из них SCORED 1066, ERROR 1019, UNCERTAIN 814, NEW 402
- Позиций: 10628, документов: 13182
- Черновиков писем: 283, из них отправлено 10
- КП: 3, поставщиков: 14
