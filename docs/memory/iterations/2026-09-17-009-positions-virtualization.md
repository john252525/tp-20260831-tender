# Iteration 009 — Виртуализация списка позиций
Date: 2026-09-17

## Goal
Карточка тендера не открывалась, если у закупки много позиций. Браузер падал
по памяти: ядро убивало процесс (`Out of memory: Killed process chrome-headless`).
Причина: таблица рендерила все строки сразу (`positions.map()` без ограничения),
а в базе есть закупки с 11 014 позициями.

## Changes
- frontend/src/pages/TenderDetailPage.tsx — TenderPositionsTab показывает
  позиции порциями по 200 с кнопкой «Показать ещё» и счётчиком
  «Показано N из M». Хук useState поднят до раннего return (правила хуков).
  CSV-экспорт по-прежнему отдаёт ВСЕ позиции, а не только видимые
- frontend/e2e/live-card-check.cjs — переиспользуемая проверка карточки
  в реальном браузере (headless shell), выбирает большую и маленькую закупку

## Tests
- E2E в реальном браузере: большая закупка (11 014 позиций) — 200 строк,
  счётчик верный, кнопка есть, итог OK; маленькая — 5 строк, итог OK
- CSV: 11 015 строк (заголовок плюс 11 014 позиций), данные не теряются
- jsdom: 4 passed
- tsc: 35 ошибок до правки и 35 после, новых нет

## Notes / next
- Масштаб проблемы: 7 закупок с более 500 позиций, 3 с более 2000, максимум 11 014
- Почему не нашли раньше: jsdom-тесты не воспроизводят реальную память браузера
- Playwright РАБОТАЕТ на хосте (Ubuntu 26.04, glibc): браузер в
  /root/.cache/ms-playwright/chromium_headless_shell-1234. Запускать с хоста,
  НЕ из alpine-контейнера (musl несовместим с бинарниками Chrome)
- Полный chromium на больших страницах падает по OOM, headless shell — нет
